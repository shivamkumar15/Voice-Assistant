# Core voice stack
SpeechRecognition>=3.10   # mic capture + Google Web Speech fallback STT
PyAudio>=0.2.13
pyttsx3>=2.90
piper-tts>=1.8          # neural TTS voice (espeak fallback still works without it)

# Local speech-to-text: OpenAI Whisper via CTranslate2, fully offline.
# Optional — without it ear.py falls back to the keyless Google endpoint.
# `pip install faster-whisper` on Python 3.13+ may also need
# nvidia-cublas-cu12 / nvidia-cudnn-cu12 to use a GPU; otherwise it runs on CPU.
faster-whisper>=1.1

# System control
psutil>=5.9
requests>=2.31
PyAutoGUI>=0.9.54
pypdf>=4.0

# Optional: AI chat fallback via OpenRouter (needs OPENROUTER_API_KEY)
# (uses `requests`, already listed above — no extra package needed)

# Optional: local natural-language brain — Needle 2 tool-calling model
# (github.com/cactus-compute/needle, fully offline inference)
cactus-needle>=2.0

# Optional: window management fallback where xdotool is missing
# pygetwindow
